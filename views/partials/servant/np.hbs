<div style="filter:drop-shadow(0px 0px 1px black); margin-top: -2px; margin-bottom: 50px;">
    <ul class="tabs" id="NpTabs">
        {{#each servant.noblePhantasms}}
            {{#if this.condQuestId}}
            
                {{#if this.condQuestPhase }}
                    <li class="tab col s3"><a href="#np{{@key}}">{{this.name}} ▲</a></li>
                {{/if}}

            {{else}}
            <li class="tab col s3"><a href="#np{{@key}}">{{this.name}}</a></li>
            {{/if}}
        {{/each}}
    </ul>
</div>
{{#each nps}}
<div id="np{{@key}}">
    {{{this}}}
</div>
{{/each}}